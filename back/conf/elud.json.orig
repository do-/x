{

        "listen": {
                "host"    : "127.0.0.1",
                "port"    : 8003
        },

        "db": {
                "connectionString": "postgresql://tmp:XXXXXX@localhost:5432/tmp"
        },

        "auth": {
                "allow_test_admin": 1,
                "sessions": {"timeout": 30},
                "salt_file": "/etc/tasks.salt"
        }

}